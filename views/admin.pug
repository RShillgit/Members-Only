extends layout

block content
    div(class='page-header')
        h1 Administrator 

    p *User must be authenticated to visit this page, if they are not route them to login*

    form(method='POST' action='' id='admin-form')
        label Password 
            input(type='password' name='password' required='true')
        button Become Admin

    if err
        div(class='error-message-div')
            ul
                each e in err 
                    li=e.msg  