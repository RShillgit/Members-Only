extends layout

block content
    div(class='page-header')
        h1 The Secret Club 

    p *User must be authenticated to visit this page, if they are not route them to login*

    form(method='POST' action='' id='club-form')
        label Password 
            input(type='password' name='password' required='true')
        button Join Club

    if err
        div(class='error-message-div')
            ul
                each e in err 
                    li=e.msg  