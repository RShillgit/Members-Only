extends layout

block content
    div(class='page-header')
        h1 Admin

    h1 You are now an admin

    if err
        div(class='error-message-div')
            ul
                each e in err 
                    li=e.msg  